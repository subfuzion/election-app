apiVersion: skaffold/v2beta25
kind: Config
metadata:
  name: election-app
build:
  artifacts:
    - image: gcr.io/voting-app/web
      context: src/web
      docker:
        dockerfile: Dockerfile
  tagPolicy:
    sha256: {}
deploy:
  kubectl:
    manifests:
      - kubernetes/web.yaml
